class Game {
  constructor() { //need pass in two players as arguments??
    this.humanFighter = '';
    this.computerFighter = '';
    this.winner = '';
    this.tie = false;
    this.type = ''; //ie classic or not
    this.humanWins = 0; //based off player instance
    this.computerWins = 0; //based off player instance
  }
  determineWinner() {
    //also a draw, in here?
    //need the two players as parameters?
  }
  resetBoard() {

  }
}
